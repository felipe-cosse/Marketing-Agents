# ARCH-02 verification

ARCH-02 establishes the web control surface on the repository's pinned React 19, TypeScript 6, and Vite 8 toolchain. The build gate checks the manifest against the installed React, React DOM, TypeScript, Vite, and Vite React plugin packages, executes the local compiler and bundler versions, and produces the production bundle under the exact Node version declared by the repository.

The graph remains a named, keyboard-operable visual region rather than a second ARIA tree. Its description reports the visible projection counts and directs assistive users to Tree view for complete level-by-level navigation. Every graph agent control keeps its stable accessible name while gaining a unique description containing its department, function, and level-four hierarchy context. The semantic Tree view identifies itself as the complete hierarchy-navigation model and retains the established tree-item levels and keyboard guidance.

Zero-result projections use explicit empty wording. The graph does not claim absent headings or controls, and Tree view exposes a named empty-state region tied to the zero-count summary instead of emitting an empty ARIA tree.

The focused browser journey loads the real hierarchy API through the production Vite preview. It checks all 43 source agent descriptions, duplicate Community ordinals, keyboard movement, the filtered one-department/one-function/one-agent summary, and the mutually exclusive graph-to-tree switch. Serious or critical Axe findings block both rendered states.

The canonical frontend browser aggregate includes the ARCH-02 runner, so `pnpm test:e2e` and `make web-test-e2e` retain this journey with the other committed browser requirements.

The dependency-free witness executes the projection-summary implementation under pinned Node and source-binds its graph, tree, card, stage, and application-page consumers. It also checks the committed manifest, lock importer, strict TypeScript settings, Vite React plugin, and React root entry. Rendered roles, accessible descriptions, keyboard focus, and Axe results belong to the unit and browser gates rather than source inference.

The automated browser evidence is limited to Playwright-managed Chromium and the exercised fixtures and states; it is not manual assistive-technology testing or complete accessibility conformance. WEB-01 remains authority for exact hierarchy inventory and layout, WEB-07 for the full responsive tree keyboard model, WEB-08 for broad accessibility acceptance, and WEB-09 for visual fidelity and neutral branding.

Machine authority: [`ARCH-02.json`](ARCH-02.json).
