# CAT-07 verification note

Community contains seven reusable role templates and fourteen deployment instances. Every role has exactly `.01` and `.02`; both share prompt/schema/policy through one template, use ordinal-only metadata, and keep `variant_label` null.

Machine authority: `CAT-07.json`. Runtime evidence is generated outside Git.
