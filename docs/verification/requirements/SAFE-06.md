# SAFE-06 verification note

A central immutable policy now bounds schema payloads, JSON depth, external-content volume, capability/effect allowlists, call budgets, rates, deadlines, and timeouts. Reference URLs are validated as inert provenance and expose no fetch operation.

Machine authority: `SAFE-06.json`. Runtime evidence is generated outside Git.
