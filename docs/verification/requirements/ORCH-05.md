# ORCH-05 verification note

Step input now consists only of declared admitted-input fields and schema-valid ancestor artifacts with immutable provenance references and conservative classification propagation. Root work pass-through, accumulated chat, unrelated artifacts, pointer drift, schema drift, hash drift, and central size or depth violations fail closed.

Machine authority: `ORCH-05.json`. Runtime evidence is generated outside Git.
