# OBJ-01 verification note

The application layer now exposes an immutable organization projection backed by the compiled catalog. It preserves the exact source-modeled hierarchy and fails closed on orphaned references or count drift.

Machine authority: `OBJ-01.json`. Runtime evidence is generated outside Git.
