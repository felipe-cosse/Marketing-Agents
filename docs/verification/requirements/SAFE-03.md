# SAFE-03 verification note

Source access now requires supplied input, committed fixtures, or an explicitly reviewed official API with stable resource identifiers. Generic fetch, browser, crawler, scraper, unofficial API, and reference-URL promotion fail before runtime.

Machine authority: `SAFE-03.json`. Runtime evidence is generated outside Git.
