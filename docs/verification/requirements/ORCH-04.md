# ORCH-04 verification note

The application router now snapshots only the structural catalog fields it needs, always retains the enabled eligible explicit target, and solves the remaining workflow roles with an exact set-cover dynamic program bounded to twenty slots. Cardinality wins first; workflow-declared template priority and stable catalog ordering resolve equal-cardinality covers.

The focused no-network gate exercises the compiled Community catalog directly: the `.01` course-cohort deployment covers both membership and welcome slots once, while `.02` is excluded. Negative controls reject ambiguity, catalog drift, unsupported targets or triggers, and absent capability coverage without accepting payload, chat, model output, persistence, provider, connector, or repository inputs.

Exact admitted trigger/source/configuration validation remains sealed by ORCH-02 and must be revalidated by planner integration before routing. Effective connector bindings are checked during plan materialization and action proposal; selecting a template capability is never dispatch authorization.

Machine authority: `ORCH-04.json`. Runtime evidence is generated outside Git.
