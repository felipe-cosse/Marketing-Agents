# WEB-07 verification

WEB-07 gives the organization chart a responsive hierarchy contract. The production viewport policy defaults to the verified graph above 720 CSS pixels and to one semantic tree at or below that breakpoint. A user may explicitly switch either viewport to graph or tree for the life of the mounted page. Both representations consume the same filtered, source-ordered hierarchy; conditional mounting keeps the inactive representation out of the interactive and accessibility trees, and the mode control restores focus after a switch.

The pure tree model derives one `Marketing Agents` root, five departments, twelve functions, and forty-three deployed instances from the active projection. It retains authoritative order and assigns each node stable parent, level, position-in-set, set-size, child, expansion, and deployment metadata. Root and departments begin expanded so all function groups are discoverable without exposing forty-three tab stops. Expanding every branch produces all 61 nodes, including both Community deployments as separate instances.

`OrgTreeFallback` is the narrow-screen interaction authority. It exposes one `tree`, one roving `treeitem` tab stop, and the documented ARIA expansion and hierarchy metadata. Up/Down traverse the complete visible order; Left collapses or moves to the parent; Right expands or moves to the first child; Home/End move to the visible bounds; Enter/Space select an instance; `/` moves to search; and normalized printable typeahead wraps through visible labels. Search and filters rebuild the tree from the same retained-ancestor projection rather than maintaining a second result model.

The narrow application shell keeps the exact primary-navigation labels, search, filter entry point, and `Local identity — not production authentication` warning available. Labeled filters and agent details reflow as full-width sheets, tree rows and interactive controls retain at least 44 by 44 CSS pixels, and the page does not acquire horizontal overflow. The approval route also stacks its filters, queue, review pane, and actions into one contained mobile column after removal of the legacy global width floor. An explicit graph choice remains available on mobile as a readable pannable surface rather than auto-fitting the complete graph into miniature cards.

The focused unit gate selects all Vitest cases carrying the `WEB-07` marker. The dependency-free witness executes the pure production tree and responsive-policy modules under pinned Node 24.20.0, proves exact 1/5/12/43 construction, visible-node expansion, navigation, typeahead, breakpoint and override behavior, and source-binds those modules to the tree component and page integration. The browser gate rebuilds the production Vite application and exercises the narrow tree, keyboard and touch paths, filters, full-width detail, graph opt-in, focus restoration, viewport geometry, and loopback-only network boundary.

Playwright-managed Chromium uses viewport emulation rather than a physical device or other browser engines. WEB-08 retains exhaustive axe, contrast, reduced-motion, 200-percent reflow, and cross-route accessibility acceptance; WEB-09 retains final visual-fidelity and neutral-branding sign-off. The graph/tree override is deliberately memory-only, and the browser journey reads the local API without mutating runtime, provider, or connector state.

Machine authority: [`WEB-07.json`](WEB-07.json).
