# CAT-04 verification note

The Social media catalog slice enumerates the source's 12 exact roles across New content (6), Research (2), and Tracking & analysis (4). Each has one stable `.01` instance, deterministic inert instructions, and bounded input/output schemas. No Social template is assigned a publish capability.

Machine authority: `CAT-04.json`. Runtime evidence is generated by the requirement validator and is not committed.
