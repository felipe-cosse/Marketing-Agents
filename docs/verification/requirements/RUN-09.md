# RUN-09 verification note

Action-integrity checks now invalidate any semantically changed proposal, irreversibly supersede the prior generation, and issue exactly the next generation under the same logical scope. Canonical JSON key order and Unicode normalization do not cause false invalidation.

Machine authority: `RUN-09.json`. Runtime evidence is generated outside Git.
