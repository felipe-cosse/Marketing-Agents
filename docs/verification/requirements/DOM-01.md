# DOM-01 verification note

All named core concepts now have immutable, framework-independent domain entities with stable identity, UTC, digest, lineage, revision, expiry, idempotency, timezone, and finite-policy construction checks.

Machine authority: `DOM-01.json`. Runtime evidence is generated outside Git.
