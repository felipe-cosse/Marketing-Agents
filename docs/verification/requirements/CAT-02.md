# CAT-02 verification note

The compiler now treats the Community `7 templates / 14 instances` distinction as a production semantic contract, including the exact `6/6/2` instance distribution and `3/3/1` template distribution. Mutation tests reject missing, extra, reassigned, and non-Community duplicate deployments.

Machine authority: `CAT-02.json`. Runtime evidence is generated outside Git.
