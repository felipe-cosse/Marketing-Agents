# SAFE-08 verification note

Artifacts now carry immutable source/parent lineage, exact catalog and configuration revisions, provider versions, output schema identity, UTC creation time, effective sensitivity, and a canonical domain-separated payload hash.

Machine authority: `SAFE-08.json`. Runtime evidence is generated outside Git.
