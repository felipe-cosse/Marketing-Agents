# RUN-07 verification note

Approval requests now snapshot one immutable proposed action and its policy context. A different action ID or any canonical action mutation invalidates the binding, even when the visible payload looks identical.

Machine authority: `RUN-07.json`. Runtime evidence is generated outside Git.
