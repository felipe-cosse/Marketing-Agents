# CAT-06 verification note

The Email slice preserves five source roles across Newsletter (2) and Lifecycle marketing (3). Subscribe, unsubscribe, and CRM upsert authority is explicit and approval-bound; welcome and churn content remains an inert draft with no email-send capability.

Machine authority: `CAT-06.json`. Runtime evidence is generated outside Git.
