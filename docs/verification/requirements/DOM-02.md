# DOM-02 verification note

The catalog compiler now couples each role's stable hierarchy identity, exact display name, source purpose, and local instructions. Mutation tests reject namespace mismatches, whitespace-only core fields, prompt identity drift, and sibling ordering collisions.

Machine authority: `DOM-02.json`. Runtime evidence is generated outside Git.
