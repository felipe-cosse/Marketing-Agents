# SAFE-05 verification note

The provider-neutral LLM contract now structurally separates trusted system policy from retrieved content and connector observations. It accepts structured output only and deliberately exposes no messages, tools, or model-selected tool call surface.

Machine authority: `SAFE-05.json`. Runtime evidence is generated outside Git.
