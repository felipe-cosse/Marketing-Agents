# RUN-05 verification note

Every write-bearing plan now registers a complete proposal epoch atomically. Each action derives a domain-separated key from the stable seven-field action material and persists the original exact envelope, semantic and exact hashes, redacted projection, approval policy, connector contract, bounded attempt policy, and immutable key. Regenerated random identities are non-authoritative on semantic replay: the repository returns the originally stored set in stable step order and rejects partial or changed sets.

Dispatch uses three committed phases: an exact parent-Run/action claim, a second pre-call fence, and completion from an authoritative durable connector receipt. No dispatcher transaction remains open across the connector await. Durable receipt reconciliation precedes retry or outcome-unknown classification, including a crash or lost response on the final attempt. Only a persisted `required` or `supported` idempotency contract may replay the same key after a stale call marker; an unavailable provider never receives a blind retry. Domain and database constraints cap all actions at ten attempts.

File-backed SQLite tests cover multi-action restart/replay, two-session insertion and dispatch races, outer rollback, partial-set corruption, parent cancellation fences, stale lease generations, crash windows, final-attempt receipt reconciliation, unavailable-provider recovery, exact receipt projection, action/reservation tamper resistance, process-local ledger rejection, and raw secret/cause suppression. Machine authority: `RUN-05.json`. RUN-08/ORCH-08 retain approval persistence and reservation foreign-key ownership, ORCH-09 retains general audit timeline ownership, and DEL-04 retains migration ownership.
