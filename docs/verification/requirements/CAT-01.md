# CAT-01 verification note

The first authoritative catalog release activates all five reviewed department slices under the compiler's fixed `5/12/36/43` and `12/6/5/14/6` contracts. The release resolves 36 prompts, 72 schemas, all capabilities and policies, and produces a deterministic domain-separated content hash.

Machine authority: `CAT-01.json`. Runtime evidence is generated outside Git.
