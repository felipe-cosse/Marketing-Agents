# SAFE-01 verification note

The local composition root now identifies deterministic mock adapters and an explicit dry-run status. Unregistered real selections are errors and cannot silently fall back to a mock implementation.

Machine authority: `SAFE-01.json`. Runtime evidence is generated outside Git.
