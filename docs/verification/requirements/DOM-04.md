# DOM-04 verification note

The compiler now evaluates each role's tools, triggers, effect classification, approval, retry, timeout, budget, rate limit, source references, and implementation notes as one authority contract. Assigned writes require idempotency, exact human approval, and no blind retry.

Machine authority: `DOM-04.json`. Runtime evidence is generated outside Git.
