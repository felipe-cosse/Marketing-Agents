# ORCH-01 verification note

The frontend now carries one canonical Marketing Agents root and a separately identified Marketing Orchestrator control-plane marker. The marker is visibly attached to both hierarchy representations, declares that it is not counted as an instance, and neither reserved UI identity can enter any normalized source hierarchy node.

Focused component, dependency-free connection, and real-browser gates prove the same boundary across desktop graph and narrow semantic-tree modes. Machine authority: `ORCH-01.json`. Runtime evidence is generated outside Git.
