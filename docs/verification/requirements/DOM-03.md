# DOM-03 verification note

All 36 templates now resolve distinct stable input/output schema pairs. The compiler requires Draft 2020-12 identities, object-shaped required fields, closed objects, and finite arrays/strings recursively. Positive and negative payloads exercise every one of the 72 schemas.

Machine authority: `DOM-03.json`. Runtime evidence is generated outside Git.
