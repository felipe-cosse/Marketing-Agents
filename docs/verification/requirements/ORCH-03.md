# ORCH-03 verification note

Workflows now expose a validated routing-free DAG with deterministic topological order, explicit roots and terminal results, transitive ancestor queries, and a semantic topology hash. Invalid edges, cycles, limits, dead paths, and accidental routing fields fail closed.

Machine authority: `ORCH-03.json`. Runtime evidence is generated outside Git.
